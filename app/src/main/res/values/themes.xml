<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light Mystical Theme -->
    <style name="Base.Theme.EschatonDeckTraker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        
        <!-- Secondary colors -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        
        <!-- Background colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnBackground">@color/on_background</item>
        <item name="colorOnSurface">@color/on_surface</item>
        
        <!-- Other colors -->
        <item name="colorError">@color/error</item>
        <item name="android:statusBarColor">@color/primary_variant</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Popup menu styling (affects overflow menu only, not dialogs) -->
        <item name="actionOverflowMenuStyle">@style/CustomOverflowMenuStyle</item>
        <item name="android:actionOverflowMenuStyle">@style/CustomOverflowMenuStyle</item>
    </style>

    <style name="Theme.EschatonDeckTraker" parent="Base.Theme.EschatonDeckTraker" />
    
    <!-- Custom overflow menu style (targets action bar overflow menu specifically) -->
    <style name="CustomOverflowMenuStyle" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@color/surface</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>